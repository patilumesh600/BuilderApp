// import { selectBusinessSearchDomain } from '../selectors';

describe('selectBusinessSearchDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
