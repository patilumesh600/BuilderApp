// import { selectPaymentHistoryDomain } from '../selectors';

describe('selectPaymentHistoryDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
