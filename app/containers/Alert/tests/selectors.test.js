// import { fromJS } from 'immutable';
// import { selectAlertDomain } from '../selectors';

describe('selectAlertDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
